{
	"name": "ggplot-census",
	"build": {
		// Build from Dockerfile so all R/Python deps are pre-baked into the image.
		// Enable Codespaces Prebuilds in repo Settings → Codespaces for instant startup.
		"dockerfile": "Dockerfile",
		"context": ".."
	},
	"customizations": {
		"vscode": {
			"extensions": [
				"GitHub.copilot",
				"ms-python.python",
				"ms-python.vscode-pylance",
				"cweijan.vscode-office",
				"ms-toolsai.jupyter",
				"REditorSupport.r"
			]
		}
	},
	"hostRequirements": {
		"cpus": 4
	}
}
