{
	"name": "ggplot-census",
	// Pre-built image with R + Python deps baked in — no build step on Codespaces startup.
	// To rebuild: docker build, push to dmil/r-and-python-test:latest, then reopen Codespace.
	"image": "dmil/r-and-python-test:latest",
	"customizations": {
		"vscode": {
			"extensions": [
				"GitHub.copilot",
				"ms-python.python",
				"ms-python.vscode-pylance",
				"cweijan.vscode-office",
				"ms-toolsai.jupyter",
				"REditorSupport.r"
			]
		}
	},
	"hostRequirements": {
		"cpus": 4
	}
}
